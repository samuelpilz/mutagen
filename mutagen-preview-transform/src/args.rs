//! parse arguments for the `#[mutate]` attribute

use proc_macro2::TokenTree;

/// parse the arguments of the `#[mutate]` attribute
pub fn args(args: proc_macro2::TokenStream) {
    // WIP: parse args and construct config
}
